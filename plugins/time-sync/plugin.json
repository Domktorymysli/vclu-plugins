{
  "id": "time-sync",
  "name": "Time Sync",
  "version": "1.0.0",
  "description": "Synchronizes system time from internet time servers (WorldTimeAPI)",
  "author": "vCLU",
  "license": "MIT",
  "minVcluVersion": "0.1.0",
  "tags": ["time", "ntp", "sync", "clock"],
  "configSchema": [
    {
      "key": "timezone",
      "type": "string",
      "label": "Timezone",
      "description": "IANA timezone (e.g., Europe/Warsaw, America/New_York)",
      "required": false,
      "default": "Europe/Warsaw"
    },
    {
      "key": "interval",
      "type": "number",
      "label": "Sync interval (seconds)",
      "description": "How often to sync time (default: 3600 = 1 hour)",
      "required": false,
      "default": "3600"
    },
    {
      "key": "autoSync",
      "type": "boolean",
      "label": "Auto sync on start",
      "description": "Automatically sync time when plugin starts",
      "required": false,
      "default": "true"
    }
  ]
}
